#ifndef NORETURN_H
#define NORETURN_H

#include <stdio.h>

int fnprime( FILE *stream, long double start );
int nprime( long double start );
int fprime( FILE *stream );
int prime();

#endif

// vim:ts=4
